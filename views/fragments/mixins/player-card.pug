mixin player-card(player)
    card.player-card(id="p" + player.id)
        p.card-title #{player.discord.displayName}
        p Name: #{player.name}
        p School: #{player.school}
        p Status: #[span(class="badge badge-" + player.status.toLowerCase()) #{player.status}]
        if block
            block