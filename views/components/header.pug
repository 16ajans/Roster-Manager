- avatar = typeof user !== 'undefined' ? `https://cdn.discordapp.com/avatars/${user.discord}/${user.avatar}.png` : '/images/avatar.png'
header
    a.flexH(href='/')
        img#logo(src='/images/CVRE.png' alt='CVRE Logo')
        h1 Roster Manager
    a.flexV(href='/account')
        img#avatar(src=avatar alt='Discord Avatar')